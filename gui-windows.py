from tkinter import *

window = Tk() # instantiate an instance of a window

window.geometry("500x500") # set the size of the window

window.title("My first GUI window") # set the title of the window

window.configure(background="light blue") # set the background color of the window

window.mainloop() #place the window on the screen





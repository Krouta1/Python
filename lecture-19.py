#index operator [] = gives access to a sequence elements (str,lists,tuples)

name = "petr Kroutil!"

#if(name[0].islower()):
    #name = name.capitalize()
    
#first_name = name[:4].upper()
#last_name = name[5:].upper()
last_character = name[-1]
print(last_character)
